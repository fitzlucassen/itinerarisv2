import { SearchCurrencyPipe } from './search-currency.pipe';

describe('SearchCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
